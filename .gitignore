venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
logs/
.vscode/
scratch/
*.log

# Claude Code — local AI assistant workspace (never commit)
.claude/

# YOLO model weights (large binary files — download on first run)
*.pt
*.onnx

# Training datasets (use Roboflow/DVC instead)
training/datasets/

# Recorded sessions (can be large)
recordings/
